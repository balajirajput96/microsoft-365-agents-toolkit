{
  "servers": {
    // This is the MCP server configuration
    // To add a new MCP server to declarative agent, add a new entry here
    // and run "ATK: Update Action with MCP" from command palette or directly from CodeLens
    "mslearn": {
			"url": "https://learn.microsoft.com/api/mcp",
			"type": "http"
		}
  }
}